# Instrucciones de Ejecución - AutoQA Pro

Has copiado exitosamente el proyecto a tu escritorio. Para ejecutarlo, sigue estos pasos:

## 1. Estado de PostgreSQL
¡Buenas noticias! Tu PostgreSQL ya está corriendo (versión 15).
El error de autenticación es solo por las credenciales en el archivo `.env`.

## 2. Solución de Credenciales (Error P1000)
1. Abre el archivo: `/Users/franciscotorres/Desktop/ia-automation-test/backend/.env`
2. Modifica la variable `DATABASE_URL`.

Prueba estas opciones comunes en Mac (guarda y prueba `npx prisma db push` después de cada cambio):

**Opción A (Tu usuario de sistema, sin contraseña):**
`postgresql://franciscotorres@localhost:5432/autoqa_pro?schema=public`

**Opción B (Usuario postgres, sin contraseña):**
`postgresql://postgres@localhost:5432/autoqa_pro?schema=public`

**Opción C (Tu usuario, contraseña 'postgres'):**
`postgresql://franciscotorres:postgres@localhost:5432/autoqa_pro?schema=public`

## 3. Instalación de Dependencias
```bash
cd backend
npm install
npx prisma generate
npx prisma db push  # Si esto funciona, ¡ya estás listo!

cd ../frontend
npm install
```

## 4. Iniciar el Proyecto
Vuelve a la carpeta raíz (`ia-automation-test`) y ejecuta:

```bash
./start-all.sh
```
